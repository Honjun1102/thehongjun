<footer class="shell footer">
  <div class="footer-card card">
    <p class="eyebrow">One Person, Four Layers</p>
    <div class="footer-grid">
      <div>
        <h2>Let meaning return to the human being itself.</h2>
        <p>
          This is a writer's home about body, mind, livelihood, and trust. It is not a
          content feed. It is an entrance back to a steadier, more sovereign way of
          being alive.
        </p>
        <div class="footer-actions">
          <a class="footer-link" href="/en/support/">Support the Writing</a>
        </div>
      </div>
      <div class="footer-note">
        <span>01</span>
        <p>First rebuild the body, then sharpen the mind, then secure livelihood, and finally learn how to build trust.</p>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    padding: 0 0 3rem;
  }

  .footer-card {
    padding: 1.75rem;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.8fr) minmax(18rem, 0.8fr);
    gap: 2rem;
    align-items: end;
  }

  .footer h2 {
    margin: 0 0 0.75rem;
    font-family: var(--font-display);
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    line-height: 1.1;
  }

  .footer p {
    margin: 0;
    color: var(--ink-muted);
    line-height: 1.8;
  }

  .footer-actions {
    margin-top: 1rem;
  }

  .footer-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.72rem 1rem;
    border-radius: 999px;
    border: 1px solid rgba(23, 21, 18, 0.12);
    color: var(--ink);
    background: rgba(255, 252, 247, 0.7);
    font: 500 0.8rem/1 var(--font-mono);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    transition:
      transform 160ms ease,
      border-color 160ms ease,
      background-color 160ms ease;
  }

  .footer-link:hover {
    transform: translateY(-1px);
    border-color: rgba(155, 77, 34, 0.3);
    background: rgba(255, 252, 247, 0.92);
  }

  .footer-note {
    display: grid;
    gap: 0.75rem;
    padding-left: 1.25rem;
    border-left: 1px solid var(--line);
  }

  .footer-note span {
    color: var(--accent);
    font: 500 0.82rem/1 var(--font-mono);
    letter-spacing: 0.14em;
  }

  @media (max-width: 820px) {
    .footer-grid {
      grid-template-columns: 1fr;
    }

    .footer-note {
      padding-left: 0;
      padding-top: 1rem;
      border-left: 0;
      border-top: 1px solid var(--line);
    }
  }
</style>
